# Calculator

Simple web-based calculator (HTML/CSS/JS). Supports add, subtract, multiply, divide.

## Run locally
Open `index.html` in browser or serve with a static file server.

## Features
- Basic arithmetic
- Decimal support
- Clear (C)
